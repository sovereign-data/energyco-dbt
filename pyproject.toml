[project]
name = "energyco-dbt"
version = "0.1.0"
description = "EnergyCo demo — dbt medallion over landed smart-meter data"
requires-python = ">=3.11"
dependencies = [
    "dbt-core>=1.11.7",
    "dbt-trino>=1.10.1",
]
